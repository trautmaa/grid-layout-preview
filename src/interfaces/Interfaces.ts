export type ColumnOptions = number;

export interface ConfigOptions {
	base: number;
	padding: number;
	baseColumns: number;
	strokeWidth: number;
	strokeDashArray: number;
	fill: string;
	stroke: string;
	viewBox: string;
}
